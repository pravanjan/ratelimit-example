plugins {
    // Support convention plugins written in Kotlin.
    // Convention plugins are in build scripts in 'src/main' that automatically become available in the main build
    `kotlin-dsl`
}

repositories {
    gradlePluginPortal()
}